interface actionType {
    type: string,
    payload: any
};

const initialState = {
    
};

const reducer = (state = initialState, action: actionType) => {
    return state;
};

export default reducer;